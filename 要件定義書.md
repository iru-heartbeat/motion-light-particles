# 要件定義書

## 1. プロジェクト概要
- 背景: 以前作成した「マウスに引き寄せられる光の粒子アート」の発展形として、触れずに操作できる体験を作りたい。人に見せたときに驚いてもらえるデモ・ポートフォリオ用コンテンツが欲しい。
- 目的: Webカメラの前で手を振るだけでパーティクルが反応する、非接触型のインタラクティブなビジュアル体験を実現する。
- スコープ:
  - 含むもの: Webカメラでの動き検知、検知座標へのパーティクル発生・描画、カメラ拒否時のフォールバック
  - 含まないもの: 顔認識・骨格推定など機械学習モデルを用いた高度なトラッキング、映像の録画・保存・外部送信、モバイル向けの専用最適化

## 2. ユーザーストーリー

As a このデモを見せられる相手,
I want カメラの前で手を振るだけでパーティクルが反応するのを見られること,
So that 触れずに操作できる面白さを体感できる.

As a 作者（いるまるさん）,
I want 単一HTMLファイルで完結する実装にしたい,
So that ポートフォリオサイトへの埋め込みや共有がすぐにできる.

## 3. 機能要件

| ID | 機能名 | 説明 | 優先度 | Phase |
|---|---|---|---|---|
| F-01 | カメラ許可要求 | ページ読み込み時に `getUserMedia` でカメラアクセスを要求する | 高 | MVP |
| F-02 | 動き検知 | 前フレームと現フレームの画素差分から、動きのあった領域をブロック単位で検出する | 高 | MVP |
| F-03 | パーティクル発生 | 検知した座標付近にパーティクルを発生させ、canvasに描画する | 高 | MVP |
| F-04 | パーティクル減衰 | 発生したパーティクルが時間経過でフェードアウト・消滅する | 高 | MVP |
| F-05 | 拒否時フォールバック | カメラ許可を拒否された場合、案内メッセージを表示しマウス操作版に切り替える | 中 | MVP |
| F-06 | 感度調整 | 差分検知のしきい値を変数化し、環境（照明など）に応じて調整できるようにする | 中 | Phase2 |

## 4. 非機能要件
- パフォーマンス: 一般的なノートPCで滑らかな描画（体感30fps程度）を維持する。差分計算は毎フレームではなく間引き実行（例：3フレームに1回）でCPU負荷を抑える
- セキュリティ・プライバシー: カメラ映像は外部送信・保存を一切行わない。映像そのものも画面には表示しない（動き検知の内部処理でのみ使用）
- 可用性: 個人ポートフォリオへの埋め込み用途のため、SLAなどの可用性要件は設定しない
- スケーラビリティ: サーバー通信を伴わないブラウザ完結型のため、同時接続数などのスケーラビリティ要件は対象外

## 5. 制約条件
- 予算: 無料枠で完結（外部API・有料ライブラリは使用しない）
- 期間: 実装は1時間程度を目安とする
- 技術: HTML + Vanilla JavaScript（canvas API, `getUserMedia`）による単一ファイル構成。Claude Codeで実装する

## 6. 画面遷移

```
[ページ読み込み]
      |
      v
[カメラ許可ダイアログ]
   /        \
 許可         拒否
  |            |
  v            v
[パーティクル画面]  [案内メッセージ表示]
（カメラ動き検知で反応）    |
                          v
                 [マウス操作フォールバック画面]
                 （前作のマウス版ロジックを流用）
```

## 7. 参考デザイン
前作「マウスに引き寄せられる光の粒子アート」（`light_particles.html`）の配色・パーティクル表現をベースに、入力元をマウス座標からカメラの動き検知座標へ置き換える形で拡張する。
